# 散户温度计 V2 升级计划

目标：在现有本地 A 股社区情绪看板基础上，吸收 `mom-index` 与其进阶版的交互方式，升级为“每日累积数据 + 多视图研究工作台”。本次重点不是复制第三方页面，而是把可验证、可持续更新的功能落到本项目自己的数据模型中。

## 参考拆解

参考来源：

- 本地 `D:\github\mom-index`：每日 `data/history.json`、`data/dashboard_data.json`，四板块指数卡、买入/卖出子指数、典型帖子和 Chart.js 历史曲线。
- 进阶版 `mom-index-eight.vercel.app/dashboard.html`：股票/板块查询、卡片微型趋势、实时评论刷新、AI 智能解读、板块联动热力图、30 天散户情绪日历、问答/毒舌所。

借鉴原则：

- 借鉴信息架构和交互，不复制代码、文案、品牌或受限数据。
- 不绕过登录、验证码、反爬或第三方限制。
- “宝妈/韭菜/小白”只描述社区表达风格，不推断真实身份。
- 所有估算、代理指标和数据缺失都要在界面上标明。

## V2 功能范围

### 1. 每日数据累积与兼容层

- [x] 新增 `public/data/daily/YYYY-MM-DD.json` 每日快照目录。
- [x] `public/data/index.json` 保存可用日期、数据版本和生成状态。
- [x] 更新脚本生成每日文件，并同步更新 `latest.json`；同日重复运行覆盖同日文件。
- [x] 读取旧版 `latest.json/history/sectorHistory`，并合并已落盘每日快照，重启后平滑恢复历史。
- [x] 保留 60 日历史；存在缺失日期时显示缺失，不伪造 0。

### 2. 市场总览

- [x] 综合情绪、讨论热度、赚钱效应代理、涨跌广度、主力净流入覆盖。
- [x] 全市场 10/20/60 日趋势，支持温度、热度、赚钱效应和方向切换。
- [x] 今日高频表达、实时采集状态、来源覆盖、置信等级。
- [x] 30 日散户情绪日历：日期 × 情绪级别，支持 hover 查看温度、热度、样本量和记录类型。

### 3. 细分板块研究

- [x] 继续使用 24 个细分主题，按科技、高端制造、金融地产、新能源车、消费医药、周期防御分组。
- [x] 每个主题展示：情绪指数、买入/卖出子指数、当前热度、1/5 日变化、样本占比、赚钱效应、代表标的涨跌、1/5 日资金流、散户表达构成。
- [x] 板块卡片微型趋势图；按升温、热度、赚钱效应、资金流和样本占比排序。
- [x] 板块联动热力图：基于最近 30 个有观测交易日的板块温度变化计算 Pearson 相关；至少 5 个共同观测日，样本不足显示“积累中”。
- [x] 板块查询结果展示趋势、指标解释、代表帖子摘要；方法审计页展示数据源状态。

### 4. 动态查询

- [x] 支持输入股票代码、股票名称或主题名查询。
- [x] 查询结果展示匹配主题、代表标的、最近情绪指数、买入/卖出倾向、热度曲线、最近帖子摘要。
- [x] 查询只匹配本地配置和已采集数据，不承诺覆盖未配置股票。
- [x] 明确提示“单股结果是代表标的/主题代理，不等于完整板块指数”。

### 5. 评论与解释

- [x] 每个板块展示最近可用的匿名评论摘要，保留来源、时间、表达类型和命中信号。
- [x] 增加本地“毒舌所”：根据当前指标生成固定规则的短评，语气辛辣但不做买卖指令。
- [x] 增加“AI 智能解读”区域：当前使用本地模板/规则生成，不依赖 API Key，并显式标记生成来源。
- [x] 解读内容引用当前热度、赚钱效应、资金流和风险矛盾，不输出确定性预测。

### 6. 30 日联动与日历

- [x] 联动热力图显示板块两两相关性，颜色区分同步/反向/弱联动。
- [x] 提供强联动对列表并显示相关系数；共同观测日不足时明确显示积累中。
- [x] 情绪日历按温度区间显示冷清/平稳/升温/亢奋，支持 hover 查看当日摘要。

## 数据设计

每日文件建议结构：

```json
{
  "meta": { "date": "2026-09-02", "generatedAt": "...", "mode": "live", "methodVersion": "MVP-2.0" },
  "summary": { "overall": 50.0, "heat": 60.0, "profitEffect": 48.0, "breadthUpRate": 42.0 },
  "sectors": [],
  "signals": [],
  "comments": [],
  "marketStats": {},
  "diagnostics": {}
}
```

历史聚合：

- `index.json` 只做日期索引，不复制完整数据。
- 看板读取 `latest.json`（并定时刷新）；每日文件由更新脚本持久化，后端下次运行时用于重建历史。
- 相关性和日历只基于实际存在且满足最小样本的日期。

## 实施顺序

1. [x] 先完成每日文件持久化与旧快照兼容。
2. [x] 再把 24 个主题的指标和匿名评论补齐到统一 schema。
3. [x] 实现市场总览趋势、情绪日历和板块联动热力图。
4. [x] 实现本地查询与板块详情交互。
5. [x] 实现毒舌所和本地智能解读。
6. [x] 更新方法审计、README、测试和运行脚本。
7. [x] 运行真实采集、生产构建、Lint、Python/网页测试。

## 完成标准

- [x] 每次运行都会生成或覆盖对应日期的 `daily/YYYY-MM-DD.json`，并更新日期索引。
- [x] 首页首屏可看到市场总览、24 主题中至少 4 个升温/降温主题和 30 日数据状态。
- [x] 能查询已配置的股票/主题，并看到可解释的本地结果。
- [x] 能看到 30 日情绪日历和板块联动热力图；数据不足时有明确空状态。
- [x] 毒舌所/智能解读不绕过外部限制、不暴露用户身份、不输出买卖指令。
- [x] 旧版快照仍能打开；Python 测试、网页构建、Lint 全部通过。

## 不在本次范围

- 登录态小红书/抖音/微博数据接入。
- 绕过 B 站、社区或行情接口的验证码/风控。
- 自动交易、选股、仓位建议和收益承诺。
- 将单只代表标的涨跌冒充完整板块全成分收益。
- 必须依赖云端大模型才能运行的功能。
